{
  "schema": "hmc.model_manifest",
  "schema_version": 1,
  "note": "Pinned MediaPipe Tasks .task files. Workflow 3 owns real model wiring; the fixture pipeline uses the fake detector/fitter and needs no weights. Fill in url/sha256 when the real models are added, and verify checksums at startup.",
  "models": {
    "pose": {
      "filename": "pose_landmarker.task",
      "url": null,
      "license": "Apache-2.0 (MediaPipe)",
      "sha256": null
    },
    "hands": {
      "filename": "hand_landmarker.task",
      "url": null,
      "license": "Apache-2.0 (MediaPipe)",
      "sha256": null
    }
  }
}
